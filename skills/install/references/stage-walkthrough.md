# Stage: closing summary (first-session walkthrough)

Adapted from donor `skills/install/references/stage-7-walkthrough.md`'s
intent — a short, concrete "here's what you can do now" — rather than its
exact wording (donor's walkthrough assumed the 7-stage plugin-negotiation
flow this skill doesn't have).

After `skills.install.lib.record_install(framework_version)`, present:

- **`/ren:pin "<text>"`** — remember something right now, mid-session.
- **`/ren:recall <query>`** — look something up from the wiki.
- **`/ren:wrap`** — end-of-session consolidation (an L1 narrative summary, plus any durable lessons the classifier gate approves for queuing).
- **`/ren:interview`** — re-run anytime to fill in more identity detail; every question is still skippable.

If stage 5 (first project) produced a first-session artifact, reference it
here too: "here's what I captured about `<project>` — see `projects/<slug>/map.md`."

Keep this short. The point is orientation, not a feature tour.
