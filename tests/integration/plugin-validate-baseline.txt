# `claude plugin validate` baseline — captured 2026-05-29 (after C2+C4 Crucible one-repo restructure)

This file is the verbatim output of running the validator against the plugin tree. CI re-runs the equivalent command in `.github/workflows/validate.yml`. Any new warnings/errors are flagged for triage in `plugin-validate-known-issues.md`.

CC version at capture time: 2.1.156 (verified via `claude --version`)

## Command 1: marketplace validation (strict mode) — primary CI gate

```
$ claude plugin validate ./ --strict

Validating marketplace manifest: /home/hsozer/Dev/startup-framework/.claude-plugin/marketplace.json

✔ Validation passed
```

**Status**: clean. With both manifests at the repo root, `claude plugin validate ./` selects the marketplace manifest (identical behavior to Hazar's `crucible` plugin, the locked reference layout). The marketplace's `source: "./"` resolves the plugin at the repo root.

## Command 2: plugin manifest validation (strict mode)

```
$ claude plugin validate ./.claude-plugin/plugin.json --strict

Validating plugin manifest: /home/hsozer/Dev/startup-framework/.claude-plugin/plugin.json

✔ Validation passed
```

**Status**: plugin manifest clean.

## Composite state

Both validations pass with `--strict`. Zero errors, zero warnings, zero notes.

## Plugin tree assembly — Crucible one-repo layout (no symlinks)

The repo **root is the plugin** (mirrors `crucible`). There are **no symlinks** — `hooks/`, `skills/`, `feed/`, and `wiki-skeleton/` are real directories at the root, alongside `.claude-plugin/{marketplace.json,plugin.json}`:

- `.claude-plugin/marketplace.json` — marketplace manifest; single plugin entry with `source: "./"`.
- `.claude-plugin/plugin.json` — plugin manifest (source of truth for `version` + `userConfig`).
- `hooks/`, `skills/`, `feed/`, `wiki-skeleton/` — real directories, shipped verbatim.

This dissolves the pre-fix blockers C2 (missing `feed` symlink → Activity Feed `ImportError`) and C4 (dangling `../../` symlinks on copy). See `plugin-validate-known-issues.md` § DISCOVERY 2 for the history.

## Regenerating this baseline

```bash
cd /home/hsozer/Dev/startup-framework
{
  echo '# `claude plugin validate` baseline — captured '$(date -u +%Y-%m-%d)
  echo
  echo '## Command 1: marketplace validation (strict mode)'
  echo
  echo '```'
  claude plugin validate ./ --strict
  echo '```'
  echo
  echo '## Command 2: plugin manifest validation (strict mode)'
  echo
  echo '```'
  claude plugin validate ./.claude-plugin/plugin.json --strict
  echo '```'
} > tests/integration/plugin-validate-baseline.txt 2>&1
```

CI workflow `validate.yml` runs `claude plugin validate ./ --strict` on every push to main and rc, plus every PR. The CI's expectation: ZERO errors.
