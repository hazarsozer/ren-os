---
title: "Project Wiki Schema"
type: project-schema
schema_version: 1
project: {{project}}
framework_version: "{{framework_version}}"
created: {{today}}
updated: {{today}}
---

# Wiki schema — {{project}}

<!-- This is THIS project's SCHEMA document (the LLM-wiki pattern):
     the structure + conventions every session reads and follows before
     writing under projects/{{project}}/. The ingest worker drafts the real
     taxonomy from what the repo/domain actually contains — this stub only
     marks the slot. Evolving it later is a normal wiki write. -->

## Taxonomy

<!-- Which knowledge/ subdirectories exist and what belongs in each, e.g.
       knowledge/entities/…    domain objects
       knowledge/mechanics/…   how the system behaves
     Every subdirectory carries a hub index.md that links its children;
     the L2 map points at hubs and top-level pages, never deep leaves. -->

## Naming conventions

<!-- File/heading naming rules for this project's pages. -->

## Volatile facts

<!-- A fact line that drifts over time (a version number, a release count)
     can carry a `ren-volatile: <kind>` HTML-comment marker (exact syntax
     in skills/wiki-health/SKILL.md) so wiki-health's freshness sweep can
     verify and refresh it against ground truth. -->

## raw/

<!-- What projects/{{project}}/raw/ holds: source material, write-once
     by convention (fix by adding, not rewriting). Sources, not claims —
     coherence scans skip it; pointers may target it; wake-up never
     injects it. -->
