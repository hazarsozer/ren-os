---
title: "Open Work"
type: open-work
schema_version: 1
project: {{project}}
framework_version: "{{framework_version}}"
created: {{today}}
updated: {{today}}
---

# Open work — {{project}}

<!-- The project's open-thread ledger (0.6.5). One line per item:
       - [ ] <one-line description> — ptr:<plan:path#task-N | issue:#N | spec:path§X | repo:ref> (opened YYYY-MM-DD)
     Closed items keep the same line, checked, with a close date:
       - [x] <description> — ptr:<...> (opened YYYY-MM-DD, closed YYYY-MM-DD)
     /ren:wrap reconciles this page at close-out. Lines are NEVER deleted:
     closed lines older than 14 days MOVE to the archive section intact.
     Only still-unchecked lines in the open section are injected at
     wake-up.
     Keep both `## Open` and `## Archive` headers: items written above the
     open header are preserved but never parsed, so they stop being
     reconciled and stop surfacing at wake-up. -->

## Open

## Archive
